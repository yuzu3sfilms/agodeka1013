# v14.13 Evidence-First Conversation Routing

## 根本修正

- 過去トピックを現在の発言文字列へ連結する処理を廃止。
- 現在の発言、現在抽出された話題、継承された会話トピックを別々の証拠チャネルとして保持。
- Scene Replayは、現在の発言内の語が過去シーンのアンカーへ完全一致した場合のみ候補化。
- 継承トピック、話者一致、疑問文、部分文字列だけではReplayを開始できない。
- `は？`などの短い修復反応と、`やめて`などの停止発言はScene Replayへ送らない。
- Speaker Resolutionは発言者識別専用へ変更。呼称候補を生成プロンプトへ渡さない。
- fallback生成が発言者名を先頭呼称として出した場合も除去する。

## 再発防止テスト

- `は？` + previous topic `アルミホイル` -> Replayなし
- `やめて` + previous topic `アルミホイル` -> stop/silence、Replayなし
- `アルミホイル巻かないと` -> `かないと`等の部分一致ではReplayなし
- `グランド土塚なつかしい？` -> 現在発言の完全一致アンカーに基づく実ログReplayは維持
- 発言者 Reiji Shioda のfallback候補先頭に `塩田、` が出ても除去
